apiVersion: 1
providers:  
  - name: slo-dashboards
    folder: SLO  
    folderUid: slo  
    type: file  
    disableDeletion: false  
    updateIntervalSeconds: 30  
    allowUiUpdates: false  
    options:
      path: /etc/grafana/provisioning/dashboards/slo.json
